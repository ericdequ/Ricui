# Changesets placeholder

Install and initialize changesets when release automation is enabled:

```bash
npm install -D @changesets/cli
npx changeset init
```

Then use changesets for package versioning and release notes.
