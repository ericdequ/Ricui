# npm auth reads the token from the NPM_ENV environment variable (set in BEV/.env
# and in Vercel global env), so no secret is hardcoded here. Public scoped
# install needs no token; publishing does.
//registry.npmjs.org/:_authToken=${NPM_ENV}
@ric:registry=https://registry.npmjs.org/
